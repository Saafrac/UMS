spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ums_db
    username: postgres
    password: ali1230123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

flyway:
  enabled: true
  locations: classpath:db/migration

security:
  jwt:
    secret: YourVerySecretKeyHere
    expirationMs: 86400000
